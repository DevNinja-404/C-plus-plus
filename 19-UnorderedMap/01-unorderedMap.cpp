#include <iostream>
#include <map>

using namespace std;

void explainUnorderedMap()
{
    // Everything is same as that of map but it doesn't store the elements in any order,it stores elements randomly
    // has unique keys though

    // the unorderedMap works in O(1),constant time but in the worst case upto O(N) whereas map works in logN time complpexity
}

int main()
{
    explainUnorderedMap();
    return 0;
}