#include <iostream>
#include <map>

using namespace std;

void explainMultiMap()
{
    // Everything is same as that of map but it can store elements with duplicate keys
    // map[key] cannot be used in multiMap.
}

int main()
{
    explainMultiMap();
    return 0;
}