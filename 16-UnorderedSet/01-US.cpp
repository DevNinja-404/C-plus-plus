#include <iostream>
#include <unordered_set>

using namespace std;

void explainUnorderedSet()
{
    // Everything is same as that of set but it only follows the ordere unique part not the orderes part
    // lower_bound() and upper_bound() function doesn't work
    // rest all functions are same as set.

    // In most cases it has better time complexity than the set except some when collision happens
}

int main()
{
    explainUnorderedSet();
    return 0;
}
